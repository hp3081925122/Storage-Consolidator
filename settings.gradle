pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.neoforged.net/releases' }
    }
}

// 固定 26.1.2 NeoForge 工程名称，避免沿用旧版本模板名称。
rootProject.name = 'storage-consolidator-2612-neoforge'
